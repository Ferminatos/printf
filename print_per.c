#include "main.h"

/**
 * print_per - a function that accounts for %% as an input
 * Return: Return the % character
 */

int print_per()
{
	int chars = 0;

	_putchar('%');
	chars++;

	return (chars);
}
